# PeggyBank — Project Status
**Last updated:** 2026-06-22 (Icon System v1.0 foundation added)
**GitHub:** https://github.com/church-1980/PeggyBank
**Local path:** C:\Users\spall\Documents\PeggyBank
**Stack:** React Native 0.83.6 · Expo SDK 55 · TypeScript · expo-sqlite · New Architecture

> This file is the source of truth for all future sessions.
> Read it at the start of every conversation before touching any code.

---

## ✅ Completed

### Design Foundation
- Full theme system in place — `useColors()` + `makeStyles(C: ColorPalette)` + `useMemo` on all screens
- Light mode, dark mode, and system mode all wired through `ThemeContext`
- No hardcoded colors remaining in any screen
- All modal dismiss buttons use `chevron-down` (not `chevron-back`)
- All non-modal navigation uses `chevron-back`
- All cards use `Radius.lg`, `paddingVertical: 14`, consistent section labels
- All modal overlays use `rgba(0,0,0,0.6)`
- All empty state icons are 64×64 with `bgElevated` background
- All outline icons — `pencil-outline`, `trash-outline`, `add-outline` etc. — consistent across app

### Dashboard
- Safe to Spend card with income, spent, available mini-row
- Payday countdown (reads from settings table)
- Suggestion card (appears only when safeToSpend > $50)
- Section order: Safe to Spend → Suggestion → **Coming Up (Bills)** → **Your Goals** ← correct
- Featured Goal uses compact `GoalProgressWidget` (not the full card)
- Empty state when no goal is pinned: flag icon + "No featured goal" + Browse button
- Unpin clears immediately with no ghost (calls `setPinnedGoals([])` before DB update)

### Goals
- Full goals list with add / edit / delete
- 20 goal types (vacation, cruise, flight, wedding, car, home, etc.) each with emoji + color + icon
- Pin / unpin from action sheet
- GoalProgressCard (full SVG card) used on Goals screen
- GoalProgressWidget (compact) used on Dashboard — name, tiny airplane→island strip, %, remaining
- `pinned` and `goal_type` columns in database via migration

### Bills
- Add / edit / delete bills
- Mark paid / unpaid
- Recurring bill flag
- Coming Up section on Dashboard shows 2 most urgent unpaid bills
- Dismiss button fixed: `chevron-down`

### All Other Screens (built and working)
- Add Expense / Add Income (modal forms)
- Expenses screen with history
- Incomes screen
- Debt screen
- Subscriptions screen
- Monthly Breakdown
- Calendar
- Payday screen
- Currency (multi-currency with rates)
- Weekly Check-In (spending mood + bills check + insights)
- Settings → Appearance (light/dark/system)
- Settings → Nav Customize (user picks 3 of 5 middle tabs)
- Settings → Export (CSV)
- Settings → Share
- Onboarding (first-run)
- More screen
- QuickAdd arc menu (5 customizable arc buttons, long-press to rearrange)

### Database
- expo-sqlite singleton with promise-lock (`getDatabase()`)
- All tables: expenses, income, savings_goals, bills, settings, debts, subscriptions, calendar_reminders
- Safe migration array (try/catch per column so reruns never crash)
- Columns added via migration: `goal_type`, `pinned`, `custom_image_uri` on savings_goals

### Testing
- 72 Jest tests — all passing
- 5 test suites: helpers, theme, database, GoalProgressWidget, DashboardOrder
- Full mock setup in `jest.setup.js` (sqlite, SVG, navigation, safe area, theme, AsyncStorage)
- 6 Maestro E2E YAML flows written (ready to run, need device)
- `TEST_REPORT.md` auto-generated by `node scripts/generate-report.js`

---

## 🎨 Icon System v1.0 (Phase 0 done — Phase 1 blocked on assets)

- **Locked visual standard:** `design/PeggyBank-Icon-System-v1.png` (premium 3D icon set)
- **Docs:** `docs/PEGGYBANK_ICON_SYSTEM.md` — standard, 25-icon inventory, generation prompt, audit, 6-phase migration plan
- **Registry:** `src/data/peggyIcons.ts` — `PeggyIconKey` (25 keys) + `PEGGY_ICONS` require() map
- **Component:** `src/components/PeggyIcon.tsx` — `<PeggyIcon name size />`
- **Prompts:** `scripts/icon-prompts.md` — 25 ready-to-paste generation prompts
- **Status:** NOT wired into any screen. Nothing imports the registry yet, so the app/tests are unaffected.
- **Phase 1 (current milestone):** generate 25 PNGs into `assets/peggy-icons/` (0/25 present). Requires an external image generator — cannot be produced in-session. Visual review before any screen wiring.
- **Do NOT** start Phase 2 (wiring) until assets exist and are approved.

## 🔄 In Progress

- **Maestro E2E flows** — YAML files written and committed, but cannot run without:
  1. Maestro CLI installed (`curl -Ls "https://get.maestro.mobile.dev" | bash`)
  2. Android phone connected via USB (Developer Mode + USB Debugging on) OR Android Studio emulator running
  3. App started with `npx expo start --android`

---

## 🐛 Known Issues

| # | Screen | Issue | Status |
|---|--------|-------|--------|
| 1 | GoalProgressCard (Goals screen) | Full SVG card may clip on small phones (5.4" screens) — STRIP_H=80, not tested on small devices | Not yet investigated |
| 2 | Currency screen | Live exchange rate API — untested in offline mode | Needs manual test |
| 3 | All screens | Dark mode not visually verified on every screen after Steps 1–5 — theme tokens correct but rendering unconfirmed | Needs manual check |

---

## 🎯 Next Priority

These are approved or likely next items — **do not build until user explicitly approves each one:**

1. **Remaining GoalProgressCard journey strip types** — Cruise, Wedding, Car, Home, Emergency fund each need their own SVG illustration (currently all use the airplane→island design). Low priority, cosmetic.
2. **Expense screen — add/edit/delete Jest tests** — currently no component tests for ExpensesScreen
3. **Bills screen — add/edit/delete Jest tests** — currently no component tests for BillsScreen
4. **Income screen — fixed vs variable mode tests** — currently no component tests for IncomesScreen
5. **Dark mode visual pass** — manually check all 22 screens in dark mode for any rendering issues
6. **Shared Household feature** — future optional multi-user mode. NOT for current build. Save for after beta.
7. **Birthday & Gifts reminders** — future lightweight yearly reminder flow. NOT for current build.

---

## 🔐 Project Safety System

### Backup Strategy
Before every task Claude will:
1. Create a `backup/pre-task-YYYY-MM-DD` branch from current main
2. Commit any uncommitted state to that branch
3. Return to main and proceed with the task

### Rollback Strategy
To undo any task and return to a known-good state:

**Roll back to a stable tag:**
```bash
git checkout stable/v1-audit        # last audit state (current)
git checkout stable/v1-testing      # after test suite added
git checkout stable/v1-dashboard    # after dashboard fixes
git checkout stable/v1-foundation   # after Steps 1-5 cleanup
```

**Roll back to a backup branch:**
```bash
git branch                          # list backup branches
git checkout backup/pre-task-YYYY-MM-DD
```

**Hard reset main to a tag (destructive — only if needed):**
```bash
git checkout main
git reset --hard stable/v1-audit
git push --force
```

### Stable Tags
| Tag | Commit | What it represents |
|-----|--------|--------------------|
| `stable/v1-foundation` | 05a37a2 | After Steps 1–5: theme + nav + card + overlay cleanup |
| `stable/v1-dashboard` | d53fc32 | After dashboard fixes: section order, compact widget, unpin |
| `stable/v1-testing` | e39abf0 | After 72-test Jest suite + Maestro flows added |
| `stable/v1-audit` | 2a585e0 | After full functionality audit |

### Session Cache Files
| File | Purpose |
|------|---------|
| `PROJECT_STATUS.md` | Overall project state — read first every session |
| `CURRENT_TASK.md` | What is being worked on right now |
| `DECISIONS.md` | Approved design decisions + things NOT changing |
| `KNOWN_ISSUES.md` | All bugs, test findings, future improvements |
| `UI_AUDIT.md` | Per-screen status, visual issues, approval state |
| `AUDIT_REPORT.md` | Full functionality audit from 2026-06-05 |

### Visual Review Workflow
When any screen changes, Claude will:
1. State the screen name and what changed
2. Explain how to open it in the app
3. List exactly what to look for
4. Wait for approval before continuing

### App Preview
To see changes on your phone:
```bash
cd C:\Users\spall\Documents\PeggyBank
npx expo start
```
Scan the QR code with Expo Go app. Navigate to the changed screen.

---

## 📦 Recent Commits

| Hash | Date | What |
|------|------|------|
| `2a585e0` | 2026-06-05 | Add full functionality audit report and CURRENT_TASK.md |
| `33ec79c` | 2026-06-05 | Add PROJECT_STATUS.md |
| `e39abf0` | 2026-06-05 | Add automated test suite: Jest, RNTL, Maestro E2E flows |
| `d53fc32` | 2026-06-05 | Fix dashboard order, unpin ghost, compact goal widget, icon consistency |
| `acd745d` | 2026-06-04 | Step 5: connect Goals to Dashboard (pin/unpin, empty state) |
| `6e6a091` | 2026-06-04 | Step 4: modal overlay standardization |
| `ee73847` | 2026-06-04 | Housekeeping — remove unused Expo boilerplate files |
| `55c5346` | 2026-06-04 | Step 3: card/row standardization |
| `05a37a2` | 2026-06-04 | Step 1+2: theme system + navigation consistency |
| `b66bc20` | 2026-06-03 | Initial commit — PeggyBank full source |

---

## 🧪 Testing Status

### Jest (runs from terminal, no device needed)
```
npm test              ← run all 72 tests
npm run test:all      ← run tests + generate TEST_REPORT.md
npm run test:watch    ← re-run on save
npm run test:coverage ← coverage report
```

| Suite | Tests | Pass |
|-------|-------|------|
| helpers.test.ts | 16 | ✅ 16/16 |
| theme.test.ts | 27 | ✅ 27/27 |
| database.test.ts | 6 | ✅ 6/6 |
| GoalProgressWidget.test.tsx | 10 | ✅ 10/10 |
| DashboardOrder.test.tsx | 13 | ✅ 13/13 |
| **Total** | **72** | ✅ **72/72** |

### Maestro E2E (requires device — not yet run)
```
maestro test maestro/   ← run all 6 flows
```

| Flow | Status |
|------|--------|
| 01_add_expense.yaml | ⏳ Not run — needs device |
| 02_add_bill.yaml | ⏳ Not run — needs device |
| 03_add_goal.yaml | ⏳ Not run — needs device |
| 04_pin_unpin_goal.yaml | ⏳ Not run — needs device |
| 05_theme_switch.yaml | ⏳ Not run — needs device |
| 06_navigation.yaml | ⏳ Not run — needs device |

### Needs Manual Testing
- Receipt camera (physical device + camera)
- Push notifications
- APK install on fresh device
- Currency rate API (requires internet)
- Dark/light mode visual rendering on all 22 screens
- Pull-to-refresh on slow connection
- Safe to Spend with $0 income and $0 expenses
- Payday countdown at end of month
- Recurring bill auto-reset at new month
- Goal deadline across time zones
- Keyboard avoidance on form screens
- Large text accessibility (system font size XL)
- Data persistence across full app restart

---

## 🗂 Key File Locations

| What | Where |
|------|-------|
| Screens | `src/screens/` |
| Components | `src/components/` |
| Database | `src/database/database.ts` |
| Theme tokens | `src/theme/index.ts` |
| Colors (light + dark) | `src/theme/colors.ts` |
| Goal types (20 types) | `src/data/goalTypes.ts` |
| Types | `src/types/index.ts` |
| Helpers | `src/utils/helpers.ts` |
| Navigation | `src/navigation/AppNavigator.tsx` |
| Theme context | `src/context/ThemeContext.tsx` |
| Nav context | `src/context/NavContext.tsx` |
| Jest tests | `src/__tests__/` |
| Jest mocks | `jest.setup.js` |
| Maestro flows | `maestro/` |
| Report generator | `scripts/generate-report.js` |

---

## 🏗 Architecture Rules (must follow in every session)

1. **Theme**: always use `useColors()` — never hardcode colors
2. **Styles**: always `useMemo(() => makeStyles(C), [C])` — never static `StyleSheet.create()` with colors
3. **Modal dismiss**: always `chevron-down` — never `chevron-back` on modal screens
4. **All screens** use `presentation: 'modal'` in AppNavigator except Onboarding and Home tabs
5. **Database**: always use `getDatabase()` singleton — never `SQLite.openDatabaseAsync()` directly
6. **Migrations**: add new columns to the migrations array in `database.ts` — never alter `CREATE TABLE`
7. **Icons**: always outline variants (`pencil-outline`, `trash-outline`) except active/selected state
8. **Empty states**: 64×64 icon container with `bgElevated` background
9. **Cards**: `Radius.lg`, `borderWidth: 1`, `borderColor: C.border`
10. **Section labels**: `Typography.label`, `C.textHint`, `textTransform: 'uppercase'`, `letterSpacing: 0.6`
